11.23
1.top导航的显示隐藏
	->给标题和选项外包一个div盒子，jq事件绑定在盒子上

2.jq的hover方法
	->换图片路径需要把url在内的地址包括起来，还有路径问题(以html为准)

3.一级菜单的添加属性和移出
	->attr('class','name');
	->remove('class')

4.ul与li的多层嵌套在没有添加类名时注意使用层次选择器

11.24
1.轮播图切换图片方法next调用带了括号导致只调用了一次

2.一级菜单与轮播区的层级问题

11.27
1.树形图的点击显示问题：注意层级间的关系
	->父级关系用index()
	->兄弟关系用next()

11.28
1.修改购物车栏overflow-y的默认滚动条样式

2.侧边栏的层级显示（放在所有节点盒子的最顶部，顺序问题）

